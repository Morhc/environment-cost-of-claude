# The two report PDFs ARE committed: they are the deliverable, and rebuilding them needs
# pandoc + xelatex + DejaVu fonts. Ignore any other stray PDFs.
*.pdf
!claude-environmental-impact-report.pdf
!opus-researcher-footprint-brief.pdf

# Python
__pycache__/
*.pyc
.venv/

# macOS
.DS_Store
*.xlsx
!data/avert_emission_rates_2023.xlsx

# user-specific machine list, and the collected cache
sources.json
data/usage_cache.json

# built app bundles: they embed an absolute path, so they are built rather than committed
*.app/
