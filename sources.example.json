{
  "_comment": "Copy to sources.json and list the machines you run Claude Code on. Local is always included. Each entry is an SSH host alias; roots are auto-discovered and scoped to $USER. Give explicit roots only if discovery misses them.",
  "remote": ["trillium"],
  "_alternative_form": [{"host": "some-cluster", "roots": ["/scratch/me/.claude/projects"]}]
}
